name: api
description: api package
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  dio: ^5.1.2
  cloudinary_public: ^0.21.0

dev_dependencies:
  http_mock_adapter: ^0.4.4
  mockito: ^5.4.0
  build_runner: ^2.4.4
  test: ^1.24.2
  lints: ^2.1.0
  domain_models:
    path: ../../packages/domain_models
